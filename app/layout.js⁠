import './globals.css';

export const metadata = {
  title: 'Trần Huy Land',
  description: 'Kho Nhà Đất Đà Nẵng',
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <head>
        <script src="https://cdn.tailwindcss.com"></script>
      </head>
      <body className="bg-[#F8FAFC] text-[#0f172a]">
        {children}
      </body>
    </html>
  );
}
