import './globals.css';

export const metadata = {
  title: 'Trần Huy Land | Kho Nhà Đất Chính Châu Cẩm Lệ Đà Nẵng',
  description: 'Mua bán nhà đất uy tín.',
};

export default function RootLayout({ children }) {
  return (
    <html lang="vi">
      <head>
        <script src="https://cdn.tailwindcss.com"></script>
      </head>
      <body className="bg-[#F8FAFC] text-[#0f172a]">
        {children}
      </body>
    </html>
  );
}
